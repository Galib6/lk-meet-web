export enum ENUM_MEETING_SESSION_TYPE {
  public = 'public',
  private = 'private',
}

export enum ENUM_MEETING_ENTRY_APPROVAL_STATUS {
  approved = 'approved',
  rejected = 'rejected',
  pending = 'pending',
}
